const Filters = () => {
	return (
		<div className="mb-8">
			<h2 className="text-lg font-semibold mb-4">Filters</h2>
			{/* todo: add filter options, maybe a predefined list of tags? */}
		</div>
	);
};

export default Filters;